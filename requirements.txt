# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.11
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.65

# Reinforcement learning stack
# NOTE: torchrl and tensordict are tightly version-coupled. The code was
# developed against the versions below; the training module contains comments
# marking the (older) torchrl 0.9.2 / tensordict 0.9.1 API for reference.
torch>=2.2
torchrl>=0.11
tensordict>=0.11
gymnasium>=0.29

# Interpretable-policy (soft / oblique tree) tooling
scikit-learn>=1.3
graphviz>=0.20      # requires the Graphviz system package for rendering

# Static array-shape typing used in a few modules
jaxtyping>=0.2
